# account

Ownership: account experience and authenticated user flows. Coordinate BetterAuth UI and session behavior with the backend contract.

Current state: folder marker only. Generic client wiring is available at `src/lib/auth-client.ts`; no auth form claims end-to-end behavior.
