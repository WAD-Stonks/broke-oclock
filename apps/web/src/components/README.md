# Components

Owns reusable app-shell UI shared across routes. `SiteHeader.vue` provides the accessible primary navigation and `AppShell.vue` provides the page container.

Do not add `DealCard.vue` yet. Its props, content hierarchy, and states are a team contract decision for the browse-map and add-deal workstreams.
